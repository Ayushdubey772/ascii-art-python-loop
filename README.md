# ASCII Art Generator using Python (Loops Only)

## 📌 Project Overview
This project generates ASCII art using Python by mapping characters such as `@`, `#`, `*`, `=`, `-`, and `.`.
The implementation uses **only loops and basic file handling**, without any external libraries.

## 🎯 Features
- Uses nested loops
- No external libraries
- Generates ASCII art in terminal
- Saves output to a text file
- Beginner-friendly and viva-ready

## 🛠️ Technologies Used
- Python
- Loops
- File Handling

## 📂 Project Structure
